<Window x:Class="Cliver.Foreclosures.ListWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cliver.Foreclosures"
             mc:Ignorable="d" 
        Title="Foreclosures" Height="auto" Width="auto" SizeToContent="WidthAndHeight"
        WindowStyle="SingleBorderWindow" 
        ResizeMode="CanResize" 
        Background="#ffffff" Topmost="False" ShowInTaskbar="True"
        FontFamily="Microsoft Sans Serif"
         AllowDrop="True"
            >
    <Grid Background="Beige">
        <DockPanel>
            <ListView DockPanel.Dock="Top" MinHeight="100" Height="300" Name="list" HorizontalAlignment="Stretch">
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn Header="Created" Width="Auto" DisplayMemberBinding="{Binding Path=Foreclosure.FILING_DATE}" />
                            <GridViewColumn Header="Id" Width="Auto" DisplayMemberBinding="{Binding Path=Foreclosure.Id}" />
                            <GridViewColumn Header="Id" Width="Auto" DisplayMemberBinding="{Binding Path=Foreclosure.Id}" >
                            </GridViewColumn>
                            <GridViewColumn>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Name="show"                       Content="Show"                       Margin="0"                       VerticalAlignment="Center"                       Click="show_Click" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView.Columns>
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="0,10,10,10" Name="new" Click="new_Click">New</Button>
                <Button Margin="0,10,10,10" Name="export" Click="export_Click">Export</Button>
                <Button Margin="0,10,10,10" Name="close" Click="close_Click">Close</Button>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
